
let names = ["Amr", "Ahmed", "mazen", "Ali"]; // Define an array containing names

console.log(names[2]); // Print the element at index 2 (the third element in the array)
console.log(names.length); // Print the length of the array, i.e., the number of elements in it

let objects = [1, 2, 3, [12, 10, 11, [6, 7, 8]]]; // Define an array containing numbers and a nested array

console.log(objects); // Print the entire array
console.log(objects[3][2]); // Access and print the third element (index 2) of the first nested array
console.log(objects[3][3][2]); // Access and print the third element (index 2) of the second nested array


let str = ['ahmed', 'khalid', 'hassen']; // Define an array with three string elements

str[0] = 'amr'; // Modify the first element (index 0) and change 'ahmed' to 'amr'

console.log(str); // Print the modified array


