

let x = 100;

console.log(typeof String(x)); // "string" => Converts `x` to a string using `String()` function
console.log(typeof x.toString()); // "string" => Converts `x` to a string using `.toString()` method
